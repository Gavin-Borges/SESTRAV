# Require review from the primary maintainer for all changes in this repository.
# The wildcard (*) ensures every pull request triggers a CODEOWNER review requirement,
# which is evaluated by OpenSSF Scorecard Branch-Protection (Tier 4).
* @Gavin-Borges
